{
  "type": "module",
  "dependencies": {
    "nodemon": "^2.0.15",
    "require": "^2.4.20",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  }
}
