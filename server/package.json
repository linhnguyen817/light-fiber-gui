{
  "type": "module",
  "dependencies": {
    "nodemon": "^2.0.15",
    "require": "^2.4.20"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd src && npm install && npm run build"
  }
}
